# Vim Tips

A few editing tricks I use daily.

1. Use `ci"` to change inside quotes
2. Visual block mode: `<C-v>` to select columns
3. Macros: `qa` to record in register a, then `@a` to replay

![[vim.js#inverted]]
